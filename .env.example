# GitHub personal access token (classic)
# Permissions needed: public_repo (read-only is enough)
# Without this you get 60 API req/hr; with it: 5000/hr
GITHUB_TOKEN=ghp_your_token_here

# SQLite database path (default: data/inreview.db)
# DB_PATH=data/inreview.db

# HTTP server port (default: 8080)
# PORT=8080

REDIS_URL=redis://localhost:6379
DATABASE_URL=postgres://postgres:postgres@localhost:5432/ngmi?sslmode=disable

# ── GitHub App Auth (new) ─────────────────────────────────────────────────────
# Create a GitHub App at github.com/settings/apps/new
# Callback URL: {BASE_URL}/auth/github/callback
# Webhook URL:  {BASE_URL}/api/github/webhook
# Permissions: Contents: read, Metadata: read, Pull requests: read
# Check "Request user authorization (OAuth) during installation"

GITHUB_APP_ID=           # Numeric App ID from App settings page
GITHUB_APP_SLUG=         # URL slug, e.g. "my-app"
GITHUB_APP_PRIVATE_KEY=  # PEM content (replace newlines with \n) or use a file path
GITHUB_APP_WEBHOOK_SECRET= # Random string for validating webhook payloads
GITHUB_OAUTH_CLIENT_ID=    # Client ID from App settings (OAuth credentials section)
GITHUB_OAUTH_CLIENT_SECRET= # Client secret from App settings
SESSION_SECRET=            # 32+ random bytes for signing sessions (openssl rand -hex 32)
BASE_URL=https://ngmi.review # Full public URL (http://localhost:8080 for local dev)
